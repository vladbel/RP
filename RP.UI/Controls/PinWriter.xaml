<UserControl
    x:Class="RP.UI.Controls.PinWriter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RP.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:design="using:RP.ViewModels.DesignTime.Controls"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="400">

    <Grid d:DataContext="{d:DesignInstance design:PinViewModel,
                        IsDesignTimeCreatable=True}">
        <TextBlock Text="{Binding PinValue}"/>
    </Grid>
</UserControl>
